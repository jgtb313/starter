/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from '../../../kubb.client'
import type { RequestConfig, ResponseErrorConfig } from '../../../kubb.client'
import type {
	ListPlansQueryResponse,
	ListPlansQueryParams,
	ListPlansHeaderParams,
	ListPlans400,
	ListPlans500,
} from '../types/ListPlans'

function getListPlansUrl() {
	const res = {
		method: 'GET',
		url: `http://localhost:4000/v1/plans` as const,
	}
	return res
}

/**
 * @description Retrieves a list of plans.
 * @summary List Plans
 * {@link /v1/plans}
 */
export async function listPlans(
	params?: ListPlansQueryParams,
	headers?: ListPlansHeaderParams,
	config: Partial<RequestConfig> & {
		client?: typeof fetch
	} = {},
) {
	const { client: request = fetch, ...requestConfig } = config

	const res = await request<
		ListPlansQueryResponse,
		ResponseErrorConfig<ListPlans400 | ListPlans500>,
		unknown
	>({
		method: 'GET',
		url: getListPlansUrl().url.toString(),
		params,
		...requestConfig,
		headers: {
			...headers,
			...requestConfig.headers,
		},
	})
	return res
}
