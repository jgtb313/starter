/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from '../../../kubb.client'
import type { RequestConfig, ResponseErrorConfig } from '../../../kubb.client'
import type {
	UpdateProfilePhoneMutationRequest,
	UpdateProfilePhoneMutationResponse,
	UpdateProfilePhoneQueryParams,
	UpdateProfilePhoneHeaderParams,
	UpdateProfilePhone400,
	UpdateProfilePhone401,
	UpdateProfilePhone403,
	UpdateProfilePhone404,
	UpdateProfilePhone409,
	UpdateProfilePhone500,
} from '../types/UpdateProfilePhone'

function getUpdateProfilePhoneUrl() {
	const res = {
		method: 'PATCH',
		url: `http://localhost:4000/v1/profile/phone` as const,
	}
	return res
}

/**
 * @description Validates the OTP sent to the user's phone and allows the user to reset their phone number.
 * @summary Update Profile Phone
 * {@link /v1/profile/phone}
 */
export async function updateProfilePhone(
	data?: UpdateProfilePhoneMutationRequest,
	params?: UpdateProfilePhoneQueryParams,
	headers?: UpdateProfilePhoneHeaderParams,
	config: Partial<RequestConfig<UpdateProfilePhoneMutationRequest>> & {
		client?: typeof fetch
	} = {},
) {
	const { client: request = fetch, ...requestConfig } = config

	const requestData = data

	const res = await request<
		UpdateProfilePhoneMutationResponse,
		ResponseErrorConfig<
			| UpdateProfilePhone400
			| UpdateProfilePhone401
			| UpdateProfilePhone403
			| UpdateProfilePhone404
			| UpdateProfilePhone409
			| UpdateProfilePhone500
		>,
		UpdateProfilePhoneMutationRequest
	>({
		method: 'PATCH',
		url: getUpdateProfilePhoneUrl().url.toString(),
		params,
		data: requestData,
		...requestConfig,
		headers: {
			...headers,
			...requestConfig.headers,
		},
	})
	return res
}
