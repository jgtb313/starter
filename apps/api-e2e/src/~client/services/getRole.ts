/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from '../../../kubb.client'
import type { RequestConfig, ResponseErrorConfig } from '../../../kubb.client'
import type {
	GetRoleQueryResponse,
	GetRolePathParams,
	GetRoleQueryParams,
	GetRoleHeaderParams,
	GetRole400,
	GetRole500,
} from '../types/GetRole'

function getGetRoleUrl({
	workspaceId,
	roleId,
}: {
	workspaceId: GetRolePathParams['workspaceId']
	roleId: GetRolePathParams['roleId']
}) {
	const res = {
		method: 'GET',
		url: `http://localhost:4000/v1/workspaces/${workspaceId}/roles/${roleId}` as const,
	}
	return res
}

/**
 * @description Retrieves a single role by their ID.
 * @summary Get Role
 * {@link /v1/workspaces/:workspaceId/roles/:roleId}
 */
export async function getRole(
	{
		workspaceId,
		roleId,
	}: {
		workspaceId: GetRolePathParams['workspaceId']
		roleId: GetRolePathParams['roleId']
	},
	params?: GetRoleQueryParams,
	headers?: GetRoleHeaderParams,
	config: Partial<RequestConfig> & {
		client?: typeof fetch
	} = {},
) {
	const { client: request = fetch, ...requestConfig } = config

	const res = await request<
		GetRoleQueryResponse,
		ResponseErrorConfig<GetRole400 | GetRole500>,
		unknown
	>({
		method: 'GET',
		url: getGetRoleUrl({
			workspaceId,
			roleId,
		}).url.toString(),
		params,
		...requestConfig,
		headers: {
			...headers,
			...requestConfig.headers,
		},
	})
	return res
}
