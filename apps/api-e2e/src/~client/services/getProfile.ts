/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from '../../../kubb.client'
import type { RequestConfig, ResponseErrorConfig } from '../../../kubb.client'
import type {
	GetProfileQueryResponse,
	GetProfileQueryParams,
	GetProfileHeaderParams,
	GetProfile400,
	GetProfile401,
	GetProfile500,
} from '../types/GetProfile'

function getGetProfileUrl() {
	const res = {
		method: 'GET',
		url: `http://localhost:4000/v1/profile` as const,
	}
	return res
}

/**
 * @description Retrieves the profile information of the authenticated user.
 * @summary Get Profile
 * {@link /v1/profile}
 */
export async function getProfile(
	params?: GetProfileQueryParams,
	headers?: GetProfileHeaderParams,
	config: Partial<RequestConfig> & {
		client?: typeof fetch
	} = {},
) {
	const { client: request = fetch, ...requestConfig } = config

	const res = await request<
		GetProfileQueryResponse,
		ResponseErrorConfig<GetProfile400 | GetProfile401 | GetProfile500>,
		unknown
	>({
		method: 'GET',
		url: getGetProfileUrl().url.toString(),
		params,
		...requestConfig,
		headers: {
			...headers,
			...requestConfig.headers,
		},
	})
	return res
}
