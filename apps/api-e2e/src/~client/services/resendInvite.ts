/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from '../../../kubb.client'
import type { RequestConfig, ResponseErrorConfig } from '../../../kubb.client'
import type {
	ResendInviteMutationResponse,
	ResendInvitePathParams,
	ResendInviteQueryParams,
	ResendInviteHeaderParams,
	ResendInvite400,
	ResendInvite401,
	ResendInvite404,
	ResendInvite500,
} from '../types/ResendInvite'

function getResendInviteUrl({
	workspaceId,
	inviteId,
}: {
	workspaceId: ResendInvitePathParams['workspaceId']
	inviteId: ResendInvitePathParams['inviteId']
}) {
	const res = {
		method: 'POST',
		url: `http://localhost:4000/v1/workspaces/${workspaceId}/invites/${inviteId}/resend` as const,
	}
	return res
}

/**
 * @description Resends an existing invite email.
 * @summary Resend Invite
 * {@link /v1/workspaces/:workspaceId/invites/:inviteId/resend}
 */
export async function resendInvite(
	{
		workspaceId,
		inviteId,
	}: {
		workspaceId: ResendInvitePathParams['workspaceId']
		inviteId: ResendInvitePathParams['inviteId']
	},
	params?: ResendInviteQueryParams,
	headers?: ResendInviteHeaderParams,
	config: Partial<RequestConfig> & {
		client?: typeof fetch
	} = {},
) {
	const { client: request = fetch, ...requestConfig } = config

	const res = await request<
		ResendInviteMutationResponse,
		ResponseErrorConfig<
			ResendInvite400 | ResendInvite401 | ResendInvite404 | ResendInvite500
		>,
		unknown
	>({
		method: 'POST',
		url: getResendInviteUrl({
			workspaceId,
			inviteId,
		}).url.toString(),
		params,
		...requestConfig,
		headers: {
			...headers,
			...requestConfig.headers,
		},
	})
	return res
}
