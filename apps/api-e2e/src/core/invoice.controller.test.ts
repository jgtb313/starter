import { describe, expect, it } from 'vitest'

import { listPlans } from '~/client/services'

describe('Invoice Controller', () => {
	it('should list invoices', async () => {
		await expect(true).toBe(true)
	})
})
