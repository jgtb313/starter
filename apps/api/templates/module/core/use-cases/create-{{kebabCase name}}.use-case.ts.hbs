import { Create{{pascalCase name}}Schema, Create{{pascalCase name}}Input, Create{{pascalCase name}}Output, {{pascalCase name}}StatusEnum } from '@starter/schema'

import { createUseCase } from '@/support/utilities'
import { IUseCaseExecute } from '@/core/shared/types'
import { {{pascalCase name}} } from '@/core/{{kebabCase name}}/domain'

const execute: IUseCaseExecute<Create{{pascalCase name}}Input, Create{{pascalCase name}}Output> =
  ({ Repositories }) =>
  async (input) => {
    const {{camelCase name}} = await Repositories.{{camelCase name}}.create(
      new {{pascalCase name}}({
        ...input,
        status: {{pascalCase name}}StatusEnum.INACTIVE
      })
    )

    return {{camelCase name}}.state
  }

export const create{{pascalCase name}} = createUseCase(execute, Create{{pascalCase name}}Schema)
