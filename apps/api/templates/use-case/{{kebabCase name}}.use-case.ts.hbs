import { createUseCase } from '@/support/utilities'
import { IUseCaseExecute } from '@/support/types'

const execute: IUseCaseExecute<{}, boolean> =
  () =>
  async () => {
    return true
  }

export const {{camelCase name}} = createUseCase(execute)
